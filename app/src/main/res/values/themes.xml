<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
      Window theme only. All real theming (color, type, dark/AMOLED, dynamic
      color) lives in Kotlin under core/theme — Compose reads none of this.
      This just has to (a) drive the native SplashScreen API so cold start
      doesn't flash a blank white window, and (b) hand off to an edge-to-edge,
      no-action-bar window before Compose takes over.
    -->
    <style name="Theme.GameSpace.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.GameSpace</item>
    </style>

    <style name="Theme.GameSpace" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>

</resources>
